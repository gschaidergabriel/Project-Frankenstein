# AI-Core / Frank — Python Dependencies
# Install: pip install -r requirements.txt

# --- Core Web Framework ---
fastapi>=0.104
uvicorn>=0.24
pydantic>=2.0

# --- HTTP Clients ---
requests>=2.31
httpx>=0.25
aiohttp>=3.9

# --- Image / Vision ---
Pillow>=10.0
pytesseract>=0.3.10

# --- Document Processing ---
pypdf>=3.0
python-docx>=1.0
PyPDF2>=3.0.0

# --- Data / ML ---
numpy>=1.24
PyYAML>=6.0

# --- Security ---
cryptography>=41.0

# --- Network ---
scapy>=2.5

# --- System monitoring ---
psutil>=5.9

# --- Voice / TTS ---
piper-tts>=1.3.0
kokoro-onnx>=0.5.0
soundfile>=0.12

# --- Writer / Document Export ---
cmarkgfm>=2024.1.0
Pygments>=2.17.0
docxtpl>=0.16.0
WeasyPrint>=60.0
Jinja2>=3.1.0
aiofiles>=23.0.0

# --- GUI (Tkinter overlay — ships with Python, no pip needed) ---
# tkinter is part of the Python standard library

# --- Optional: GTK tray icon & Writer app (system packages, not pip) ---
# PyGObject>=3.42    # apt install python3-gi python3-gi-cairo
# PySide6>=6.5       # only needed for the Qt live-wallpaper module
